import { useNavigate } from 'react-router-dom';

const Register = () => {
	const navigate = useNavigate();
	return (
        <div>
            <p>Register</p>
        </div>
    );
};

export default Register;